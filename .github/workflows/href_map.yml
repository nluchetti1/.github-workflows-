name: Update HREF Hodograph Map

on:
  schedule:
    - cron: '20 3,15 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.ACTION_TOKEN }}

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libeccodes0
        pip install xarray matplotlib cartopy metpy requests numpy cfgrib ecmwflibs

    - name: Run Plotting Script
      run: python plot_href_hodographs.py

    - name: Commit and Push New Images
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        
        # 1. Remove the junk index files so they don't cause confusion
        rm -f *.idx
        
        # 2. Add the images
        git add images/*.png
        
        # 3. Commit (only if there are actual changes)
        git commit -m "Auto-update HREF plots for $(date -u)" || echo "No changes to commit"
        
        # 4. Push
        git push
